# vicocamacho.github.io

Static CV site powered by GitHub Pages. Update `index.html` with real resume
content and adjust `styles.css` if you want to tweak the minimal styling. To
preview locally open `index.html` in a browser. Once pushed to `main`, GitHub
Pages will serve the site automatically.
